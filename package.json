{
  "name": "aumoji",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter aumoji-website dev",
    "dev:picker": "pnpm --filter aumoji-picker dev",
    "build": "pnpm --filter aumoji-picker build && pnpm --filter aumoji-website build",
    "build:picker": "pnpm --filter aumoji-picker build",
    "build:website": "pnpm --filter aumoji-website build"
  }
}
